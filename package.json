{
  "$schema": "https://json.schemastore.org/package",
  "name": "bitget-server",
  "version": "1.0.0",
  "description": "API proxy Bitget pour mon site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=16"
  }
}
